# magento 1.x shell script tax config automation example

### Just copy the script to Magento "shell" folder and from command line run:
```php tax_config.php```
### and to rollback
```php tax_config.php --rollback```
